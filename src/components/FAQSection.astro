---
interface FAQ {
  question: string;
  answer: string;
}

interface Props {
  faqs: FAQ[];
  heading?: string;
}

const {
  faqs,
  heading = 'Frequently Asked Questions',
} = Astro.props;

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqs.map((faq) => ({
    "@type": "Question",
    "name": faq.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.answer,
    },
  })),
};
---

<script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

<section class="py-16 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="text-center mb-10">
      <div class="section-divider mx-auto mb-4"></div>
      <h2 class="font-display text-3xl font-bold text-brand-dark">{heading}</h2>
    </div>

    <div class="space-y-3">
      {faqs.map((faq, index) => (
        <details
          class="group bg-white rounded-xl border border-slate-100 shadow-sm overflow-hidden"
        >
          <summary
            class="flex items-center justify-between gap-4 cursor-pointer px-6 py-4 font-semibold text-brand-dark hover:text-brand-orange transition-colors list-none"
          >
            <span class="flex items-start gap-3">
              <span class="inline-flex items-center justify-center w-6 h-6 rounded-full bg-brand-orange/10 text-brand-orange text-xs font-bold shrink-0 mt-0.5" aria-hidden="true">
                {index + 1}
              </span>
              {faq.question}
            </span>
            <svg
              class="w-5 h-5 shrink-0 text-slate-400 group-open:rotate-180 transition-transform duration-200"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <path d="M6 9l6 6 6-6"/>
            </svg>
          </summary>
          <div class="px-6 pb-5 pt-1 text-slate-600 leading-relaxed text-sm border-t border-slate-50">
            {faq.answer}
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
